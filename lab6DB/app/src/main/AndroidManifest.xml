<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kirill.lab6db">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <provider
            android:authorities="com.example.kirill.lab6db"
            android:name=".contentProvider.StudentsProvider"
            android:exported="true"></provider>
        <activity android:name=".StudentsGroupActivity" />
        <activity android:name=".StudentActivity" />
        <activity android:name=".ViewActivity"></activity>
    </application>


</manifest>